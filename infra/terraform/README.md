# GCP Terraform (unused / optional)

**Do not apply** for the current lab. Auth and files use **`infra/aws-cognito`** and **`infra/aws-s3`**. The API is **Cloudflare Tunnel** → Nest, not Cloud Run.

This folder scaffolds Cloud SQL, GCS, Artifact Registry, and Secret Manager for a separate GCP project. Kept for historical Phase 3 notes and the optional `STORAGE_DRIVER=gcs` path. See [docs/GCP_SETUP.md](../../docs/GCP_SETUP.md) and [docs/AWS_AND_IAM.md](../../docs/AWS_AND_IAM.md).
