# Flutter dart-defines (not loaded automatically). Copy into `flutter run`.
# Do not put AWS keys or tunnel credentials here.
# Lab Hosted UI defaults are also compiled into lib/config.dart — dart-defines override them.

# API_BASE_URL=http://localhost:3000
# API_BASE_URL=http://10.0.2.2:3000
# API_BASE_URL=https://api.getlakbay.com

# Public Cognito client hris-mobile (no secret). Callbacks: hris://auth
OIDC_CLIENT_ID=4ij7jqehds0m6s1ubss1aj7710
OIDC_REDIRECT_URI=hris://auth
OIDC_SCOPES=openid email profile
OIDC_AUTHORIZE_URL=https://hris-lab-mtgons5.auth.ap-southeast-1.amazoncognito.com/oauth2/authorize
OIDC_TOKEN_URL=https://hris-lab-mtgons5.auth.ap-southeast-1.amazoncognito.com/oauth2/token

# Keycloak Compose stand-in (iOS simulator / desktop) — overrides the Cognito lab defaults:
# OIDC_CLIENT_ID=hris-mobile
# OIDC_AUTHORIZE_URL=http://localhost:8082/realms/hris/protocol/openid-connect/auth
# OIDC_TOKEN_URL=http://localhost:8082/realms/hris/protocol/openid-connect/token
